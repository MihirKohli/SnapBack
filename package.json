{
  "name": "return",
  "displayName": "Return",
  "description": "Persistent, cross-restart undo/redo history (up to N snapshots per file) stored on disk.",
  "version": "0.0.1",
  "publisher": "MihirKohli",
  "repository": { "type": "git", "url": "https://github.com/mihir/return" },
  "license": "MIT",
  "engines": { "vscode": "^1.85.0" },
  "icon": "icon.png",
  "categories": ["Other"],
  "activationEvents": ["onStartupFinished"],
  "main": "./out/extension.js",

  "contributes": {
    "commands": [
      {
        "command": "return.undo",
        "title": "Return Undo",
        "category": "Return"
      },
      {
        "command": "return.redo",
        "title": "Return Redo",
        "category": "Return"
      },
      {
        "command": "return.showHistory",
        "title": "Show History & Stats",
        "category": "Return"
      },
      {
        "command": "return.clearFile",
        "title": "Clear History for This File",
        "category": "Return"
      },
      {
        "command": "return.clearAll",
        "title": "Clear ALL Persistent History",
        "category": "Return"
      }
    ],

    "keybindings": [
      {
        "command": "return.undo",
        "key": "ctrl+z",
        "mac": "cmd+z",
        "when": "editorTextFocus"
      },
      {
        "command": "return.redo",
        "key": "ctrl+y",
        "mac": "cmd+shift+z",
        "when": "editorTextFocus"
      }
    ],

    "configuration": {
      "title": "Return",
      "properties": {
        "return.maxHistory": {
          "type": "number",
          "default": 500,
          "minimum": 1,
          "maximum": 50000,
          "description": "Maximum undo/redo snapshots stored per file. Each snapshot is the full file text at that moment — large files at high values will consume significant disk space."
        }
      }
    },

    "menus": {
      "editor/context": [
        {
          "submenu": "return.submenu",
          "group": "1_modification"
        }
      ],
      "return.submenu": [
        { "command": "return.undo" },
        { "command": "return.redo" },
        { "command": "return.showHistory" },
        { "command": "return.clearFile" },
        { "command": "return.clearAll" }
      ]
    },

    "submenus": [
      {
        "id": "return.submenu",
        "label": "Return"
      }
    ]
  },

  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "package": "vsce package"
  },

  "devDependencies": {
    "@types/vscode": "^1.85.0",
    "@types/node": "^20.0.0",
    "typescript": "^5.3.0",
    "@vscode/vsce": "^3.9.1"
  }
}
